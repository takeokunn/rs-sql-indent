SELECT
  u.name,
  o.total,
  p.method
FROM
  users u
INNER JOIN orders o
  ON u.id = o.user_id
LEFT JOIN payments p
  ON o.id = p.order_id
WHERE
  o.total > 100
  AND p.status = 'completed'
ORDER BY
  o.total DESC
